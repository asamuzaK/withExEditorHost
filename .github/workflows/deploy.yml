name: deploy

on:
  release:
    types: [ created ]

defaults:
  run:
    shell: bash

jobs:
  deploy:
    name: deploy binaries
    strategy:
      matrix:
        include:
          - node-version: 14.x
            os: ubuntu-latest
            make-args: --make-args="-j4"
            target: ./pkg/linux/index
            pkg-dir: ./pkg/linux
            zip-cmd: tar -cvzf linux-x86_64.tar.gz -C ./pkg/linux LICENSE README.md README.ja.md index
            zip-file: linux-x86_64.tar.gz
            zip-mime: application/gzip
          - node-version: 14.x
            os: macos-latest
            make-args: --make-args="-j4"
            target: ./pkg/macos/index
            pkg-dir: ./pkg/macos
            zip-cmd: tar -cvzf macos.tar.gz -C ./pkg/macos LICENSE README.md README.ja.md index
            zip-file: macos.tar.gz
            zip-mime: application/gzip
          - node-version: 14.x
            os: windows-latest
            make-args: ''
            target: ./pkg/win/index.exe
            pkg-dir: ./pkg/win
            zip-cmd: zip -r win64.zip ./pkg/win
            zip-file: win64.zip
            zip-mime: application/zip
    runs-on: ${{ matrix.os }}
    env:
      GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
    steps:
      - uses: actions/checkout@v2
      - name: use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v1
        with:
          node-version: ${{ matrix.node-version }}
      - name: set up NASM
        uses: ilammy/setup-nasm@v1
      - name: install dependencies
        run: npm install
      - name: run test
        run: |
          npm run lint
          npm test
      - name: build binary
        run: |
          mkdir -p ./{bundle,pkg/{linux,macos,win}}
          npm run bundle
          npm run package -- -t ${{ matrix.target }} ${{ matrix.make-args }}
      - name: zip package
        run: |
          chmod 755 ${{ matrix.target }}
          copy ./LICENSE ./README.md ./README.ja.md ${{ matrix.pkg-dir }}
          ${{ matrix.zip-cmd }}
      - name: upload release asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        with:
          upload_url: ${{ github.event.release.upload_url }}
          asset_path: ./${{ matrix.zip-file }}
          asset_name: ${{ matrix.zip-file }}
          asset_content_type: ${{ matrix.zip-mime }}
